Module src.tau_translator_omega.core_engine.parsers.parser.infrastructure.plugin_types
======================================================================================
Plugin type definitions for infrastructure layer.

Minimal interface to avoid circular imports.

Copyright: DarkLightX / Dana Edwards

Classes
-------

`Plugin(plugin_type: str, grammar_config: Dict[str, Any] | None = None, manifest: Dict[str, Any] | None = None)`
:   Minimal plugin interface for parser infrastructure.

    ### Instance variables

    `grammar_config: Dict[str, Any] | None`
    :

    `manifest: Dict[str, Any] | None`
    :

    `plugin_type: str`
    :